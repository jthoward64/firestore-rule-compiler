import { renderSchema } from "../../src/mustache/render";

describe(renderSchema, () => {
  it.todo("should render a valid rules object correctly");

  it.todo("should render an empty object correctly (sorta)");
});